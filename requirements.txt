# =============================================================================
# NuoYi (挪移) - PDF/DOCX to Markdown Converter
# https://github.com/cycleuser/NuoYi
# =============================================================================

# ── Core dependencies (always required) ───────────────────────────────────────
marker-pdf>=1.0.0           # PDF conversion engine (surya OCR + layout detection)
PyMuPDF>=1.23.0             # PDF page counting and rendering
python-docx>=0.8.11         # DOCX document conversion
Pillow>=9.0.0               # Image processing

# ── NVIDIA CUDA Libraries (IMPORTANT for GPU acceleration) ────────────────────
# These are required for marker-pdf to work with GPU on NVIDIA cards.
# Without these, you may see "CUBLAS_STATUS_NOT_INITIALIZED" errors.
#
# IMPORTANT: PyTorch 2.10+ installs nvidia-*-cu13 packages, but some libraries
# (surya, transformers) need nvidia-*-cu12 packages for module path discovery.
# This ensures both cu12 and cu13 paths are available.
#
# Install with: pip install nuoyi[cuda]
# Or manually install the cu12 packages:
nvidia-cublas-cu12>=12.0.0
nvidia-cuda-runtime-cu12>=12.0.0
nvidia-cufft-cu12>=11.0.0
nvidia-cusolver-cu12>=11.0.0
nvidia-cusparse-cu12>=12.0.0
nvidia-curand-cu12>=10.0.0
nvidia-cuda-nvrtc-cu12>=12.0.0
nvidia-nvtx-cu12>=12.0.0
nvidia-nvjitlink-cu12>=12.0.0

# ── PDF Engines (Local, Offline) ───────────────────────────────────────────────

# marker (default, best quality) - already included above

# mineru (excellent for Chinese documents)
magic-pdf[full]>=1.0.0

# docling (balanced quality, IBM)
docling>=1.0.0

# pymupdf4llm (fastest, no OCR)
pymupdf4llm>=0.0.17

# pdfplumber (lightweight, good tables)
pdfplumber>=0.10.0

# ── PDF Engines (Cloud, API key required) ─────────────────────────────────────

# llamaparse (LlamaIndex cloud)
llama-parse>=0.1.0

# mathpix (math/scientific documents)
requests>=2.28.0

# ── GUI (optional) ────────────────────────────────────────────────────────────
PySide6>=6.5.0

# ── Development (optional) ────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0
build>=1.0.0
twine>=4.0.0
ruff>=0.1.0